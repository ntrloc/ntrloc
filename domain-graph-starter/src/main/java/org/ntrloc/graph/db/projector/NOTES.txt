This is just a temporary package -- this stuff should be in the traversal package

------ We need to get into path finding ----



------ We need to get into GraphQL projections -------

Photographer
    name
    photos(filter: { colorspace: 'B&W'})
        name
        created
    agency(edgeProperties: ['dateHired'])
        name


Overall 0.0.1 functionality:

1. Schemas
2. Mutations
3. Projections
4. Edge finding
5. Minimal UI
    a. Schema viewer
    b. Basic graph viewer (the D3 view)