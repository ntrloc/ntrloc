ntrloc:
  security:
    enabled: false
    seed-test-data: true

binary:
  storage:
    strategy: block
    block:
      location: target/test-binary-storage
      autocreate: true

logging:
  level:
    root: warn
